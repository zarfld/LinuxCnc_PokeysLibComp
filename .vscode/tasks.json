{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile HAL Component",
            "type": "shell",
            "command": "halcompile --install ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Compile and install the HAL component."
        }
    ]
}
